-- Turtle commands

idle = true
mine_data = {}

-- Main function which runs turtle updates
function tick()
    if idle then
        os.sleep(5)
    end
    print("Ticking turtle")
end

function get_info(info)
    info["fuel"] = turtle.getFuelLevel()
    info["gps"] = gps.locate(1)
end